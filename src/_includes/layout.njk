<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ title or "Recetas y Pico" }}</title>
  <meta name="description" content="{{ description or '' }}" />
  <link rel="stylesheet" href="{{ '/estilos/index.css' | url }}" />
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" />
  <script src="{{ '/scripts/index.js' | url }}" defer></script>
  <script src="{{ '/scripts/footer.js' | url }}" defer></script>
</head>
<body>
  {%- include "header.njk" -%}
  <main class="main-content">
    {{ content | safe }}
  </main>
  {%- include "footer.njk" -%}
</body>
</html>
